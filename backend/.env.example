# Copia este archivo a .env y ajusta valores según tu entorno

# App
PROJECT_NAME=MOYZA API

# Postgres (docker-compose.local.yml usa: dev/dev123/moyza)
DB_HOST=db
DB_PORT=5432
DB_USER=dev
DB_PASSWORD=dev123
DB_NAME=moyza

# Solo para desarrollo: crea tablas al iniciar (no recomendado si usas Alembic)
# DB_AUTOCREATE=true
